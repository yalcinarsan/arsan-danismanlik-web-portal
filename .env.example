# Supabase (aday havuzu) — değerleri Supabase → Project Settings → API'den al.
# Bunlar PUBLIC anahtarlar (tarayıcıda görünür, RLS korur). Cloudflare Pages
# deploy'unda da aynı iki değişkeni proje env'ine eklemen gerekir.
PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=xxxx
